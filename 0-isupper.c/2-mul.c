#include "main.h"
/**
 * mul - multiplies 2 integers
 * @a: int to multiply by b
 * @b: int to multiply by a
 * Return: the result of the operation
 */

int mul(int a, int b)
{
	int c;

	c = a * b;

	return (c);
}
